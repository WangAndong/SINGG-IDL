FUNCTION fibvel, vsys, inc, xpos, ypos, pa, rr, vrot, fibers=fibers, $
                 radians=radians
   IF NOT(keyword_set(radians)) THEN BEGIN
       incrad     = inc / !radeg
       parad      = -pa / !radeg
   ENDIF ELSE BEGIN
       incrad     = inc
       parad      = -pa
   ENDELSE



END
